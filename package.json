{
  "name": "ccx",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "SET NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && next build",
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && next start",
    "export": "SET NODE_OPTIONS=--openssl-legacy-provider && next build && next export"
  },
  "dependencies": {
    "framer-motion": "^4.1.17",
    "next": "^11.1.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "swiper": "^7.0.6"
  },
  "keywords": [],
  "description": ""
}
